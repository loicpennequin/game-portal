{
  "name": "root",
  "private": true,
  "scripts": {
    "web": "yarn workspace @gp/web",
    "db": "yarn workspace @gp/db"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron",
      "**/electron/**"
    ]
  },
  "devDependencies": {
    "yarn-run-all": "^3.1.1"
  }
}
