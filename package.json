{
  "name": "root",
  "private": true,
  "scripts": {
    "api": "yarn workspace @gp/api",
    "shared": "yarn workspace @gp/shared"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron",
      "**/electron/**"
    ]
  },
  "devDependencies": {
    "yarn-run-all": "^3.1.1"
  }
}
