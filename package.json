{
  "name": "root",
  "private": true,
  "scripts": {
    "web": "yarn workspace @gp/web"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron",
      "**/electron/**"
    ]
  },
  "devDependencies": {
    "yarn-run-all": "^3.1.1"
  }
}
